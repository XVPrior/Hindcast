# Local dev: leave VITE_API_BASE empty — Vite proxies /api/* to FastAPI.
# Production (Cloudflare Pages): set this to the Fly app URL, no trailing slash.
#   VITE_API_BASE=https://hindcast-dashboard.fly.dev
VITE_API_BASE=
